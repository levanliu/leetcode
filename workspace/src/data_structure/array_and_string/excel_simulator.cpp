// Excel-like row/column insertion and deletion simulator.
#include <cstdio>
#include <cstring>

namespace ns_data_structure_array_and_string_excel_simulator {

class Solution {
public:
    // Run the simulator (reads commands from stdin, writes to stdout).
    static int run();
};

}  // namespace ns_data_structure_array_and_string_excel_simulator
