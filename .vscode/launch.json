{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug test",
      "type": "lldb-dap",
      "request": "launch",
      "program": "${workspaceFolder}/bazel-bin/${relativeFileDirname}/${fileBasenameNoExtension}",
      "args": [],
      "cwd": "${workspaceFolder}",
      "initCommands": [
        "command source ${workspaceFolder}/.vscode/lldb_sourcemap.txt"
      ],
      "preLaunchTask": "Build test (debug)"
    }
  ]
}